/target
.env
*.p8
.DS_Store
.DS_STORE
.personal/

# Sessions (local Claude session state, auto-managed)
sessions.yaml
sessions.yaml.tmp

# Runtime-generated agent context (injected per executor node, not source code)
.skills/

# Nx / Node
.nx/
node_modules/
